<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" 
    android:background="@drawable/kuntojaba_background_testi3">

    <Button
        android:id="@+id/quotesButton"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:onClick="QuoteButton" 
        android:background="@drawable/quote_button"/>

    <TextView
        android:id="@+id/textView_quote"
        android:layout_width="80dp"
        android:layout_height="match_parent"
        android:layout_alignBottom="@+id/quotesButton"
        android:layout_alignLeft="@+id/quotesButton"
        android:layout_alignRight="@+id/quotesButton"
        android:layout_alignTop="@+id/quotesButton"
        android:hint="@string/quotes_button"
        android:textSize="12sp" />

</RelativeLayout>
